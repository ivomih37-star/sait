# Secrets & runtime state — never commit these.
.env
data/
xray/config.json
configs/xray-vless-url.txt

# Generated route lists (regenerated by update-routes.sh on the server).
configs/russia-direct.txt
configs/allowed-ips-split.txt
configs/allowed-ips-full.txt

# Real client configs (contain private keys). Keep only the *.conf templates.
configs/*.conf
!configs/client1-phone.conf
!configs/client2-pc.conf
!configs/router.conf
